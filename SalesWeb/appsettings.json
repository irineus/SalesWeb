{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "SalesWeb.Controllers": "Debug"
    },
    "ApplicationInsights": {
      "LogLevel": {
        "Default": "Debug"
      }
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings_Comment": "All connection strings were moved to the Azure KeyVault cloud.",
  "AzureKeyVault_Comment": "All IDs related to Azure Key Vault were moved to secrets project file.",
  "General_Comment": "If you forked this project, use the code as reference and see the final application at: http://salesweb.azurewebsites.net/"
}
